package com.example.paketnikapp.ui.theme

import androidx.compose.ui.graphics.Color

// Primarna modra barva
val Primary = Color(0xFF3478C0)

// Sekundarna svetlejsa in lazja barva modre
val Secondary = Color(0xFF5A92D2)

// za hajlajtse
val Tertiary = Color(0xFF79A6D2)

// bg ne cisto bela, mali odtenek modre
val Background = Color(0xFFF7FAFC)

// Temno siva barva txt
val Text = Color(0xFF333333)

// povrsinka barva za background kartic in vnosnih polj - svetlo siva
val Surface = Color(0xFFF2F2F2)

// Kontrastne barve
val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFFFFFFFF)
val OnTertiary = Color(0xFFFFFFFF)
